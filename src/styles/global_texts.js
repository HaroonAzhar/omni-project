import { css } from "styled-components/macro";

import { darkGrey } from "styles/colors";

export const label = css`
  color: ${darkGrey};
  font-size: 14px;
  font-weight: 500;
  line-height: 22px;
`;
